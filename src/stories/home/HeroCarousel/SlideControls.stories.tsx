import { SlideControls } from "@/app/components/HeroCarousel/SlideControls";
import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta<typeof SlideControls> = {
    component: SlideControls,
};

export default meta;
type Story = StoryObj<typeof SlideControls>;

export const Primary: Story = {};
